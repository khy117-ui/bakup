# 업로드한 문서가 웹에서 직접 열리는 것을 막습니다.
# 웹 루트를 public 으로 잡았다면 이 폴더는 원래 노출되지 않지만, 보험으로 둡니다.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
